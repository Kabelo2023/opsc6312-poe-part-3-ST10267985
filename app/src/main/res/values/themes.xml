<resources>
    <!-- App theme for light & auto (Material3, no action bar) -->
    <style name="Theme.SyncUp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Keep it minimal to avoid missing colors; Material sets sensible defaults -->
        <!-- If you later add custom colors, set them here (colorPrimary, etc.) -->
        <item name="android:statusBarColor">?attr/colorSurface</item>
        <item name="android:navigationBarColor">?attr/colorSurface</item>


    </style>
</resources>
